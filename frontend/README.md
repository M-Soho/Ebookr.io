Frontend reports added:

- `/reports` index page
- `/reports/drip` drip campaign report page (fetches backend `/api/reports/drip-campaigns/` and falls back to mock data)

Use `npm run dev` in `frontend` to run the Next.js dev server.
